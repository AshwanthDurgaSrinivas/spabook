const { sequelize } = require('./src/config/database'); const Employee = require('./src/models/Employee').default; const User = require('./src/models/User').default; (async () => { await sequelize.sync(); const user = await User.findOne(); if(user) { await Employee.create({ userId: user.id, department: 'Massage', designation: 'Therapist', skills: { 'massage': 5 }, hourlyRate: 50 }); console.log('Employee seeded'); } else { console.log('No user found'); } })();
